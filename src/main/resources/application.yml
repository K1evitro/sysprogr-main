spring:
  profiles:
    active: dev
  application:
    name: sysprogr
    version: 1.0.0

telegram:
  bot:
    username: systeprogr_bot
    token: 8568335401:AAFuK3rLFrEPNMPtsuzbHAOAy0WumUdsk6M
    chat-id: 906826512

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/bussensors
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: 66546A555E5A7234753778214123222A472D4B614E645262356B587032733576
  access:
    cookie_name: access_token
    duration:
      minute: 5
      second: 300
  refresh:
    cookie_name: refresh_token
    duration:
      day: 7
      second: 604800

file:
  upload-dir: uploads

upload:
  path: uploads

logging:
  file:
    name: sysprogr.log

server:
  port: 8080

---
spring:
  config:
    activate:
      on-profile: prod

server:
  port: 9001

logging:
  level:
    root: INFO
    demo: DEBUG
    org.springframework.web: INFO
    org.hibernate: WARN
    org.springframework.security: DEBUG

  file:
    name: logs/application.log
    max-size: 10MB
    max-history: 30

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"
